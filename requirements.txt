# Pinku dependencies — M4 Mac Mini (Apple Silicon / macOS)
# Install: pip install -r requirements.txt
#
# PyTorch with MPS (Metal) — install FIRST via:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   (MPS is included in the standard macOS torch build from pytorch.org)

# ── STT ──────────────────────────────────────────────────────────────────────
faster-whisper>=1.0.3          # local Whisper inference (MPS / CPU)
pyaudio>=0.2.14                # mic input
webrtcvad>=2.0.10              # voice activity detection

# ── Vision / camera ───────────────────────────────────────────────────────────
ultralytics>=8.2.0             # YOLOv8 (MPS backend via torch)
mediapipe>=0.10.14             # pose + hand landmark detection
opencv-python>=4.10.0          # frame capture and processing

# ── Web dashboard ─────────────────────────────────────────────────────────────
flask>=3.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.26.0

# ── Music generation ─────────────────────────────────────────────────────────
transformers>=4.40.0           # HuggingFace — provides MusicGen (no C build deps)
scipy>=1.12.0                  # WAV file writing
accelerate>=0.29.0             # speeds up model loading on MPS
# ABC/MIDI fallback (optional, lightweight):
#   brew install abcmidi fluidsynth fluid-soundfont-gm

# ── Speaker identification ────────────────────────────────────────────────────
resemblyzer>=0.1.1.dev0        # local speaker ID gate (Resemblyzer GE2E encoder)
                               # Enroll: python enroll_speakers.py --name <name> --record 10

# ── Optional / future ─────────────────────────────────────────────────────────
# face-recognition             # face ID (requires dlib — brew install cmake first)
# openai-whisper               # alternative to faster-whisper (slower but official)
